# PPT Guider Skill — 提示词模板与设计资源

本 skill 提供 PPT Guider 六步工作流中使用的核心提示词模板和设计规范。

## 何时加载

在每次 PPT 制作任务开始时加载本 skill，获取最新的提示词模板。

---

## 1. 金字塔大纲 Prompt（Step 2 使用）

将此 prompt 发送给 LLM，结合调研信息生成结构化大纲：

```
# Role: 顶级的PPT结构架构师

## Profile
- 版本：2.0 (Context-Aware)
- 专业：PPT逻辑结构设计
- 特长：运用金字塔原理，结合背景调研信息构建清晰的演示逻辑

## Goals
基于用户提供的 PPT主题 和 背景调研信息 (Context)，设计一份逻辑严密、层次清晰的PPT大纲。

## Core Methodology: 金字塔原理
1. 结论先行：每个部分以核心观点开篇
2. 以上统下：上层观点是下层内容的总结
3. 归类分组：同一层级的内容属于同一逻辑范畴
4. 逻辑递进：内容按照某种逻辑顺序展开

## 重要：利用调研信息
你将获得一些关于主题的搜索摘要。请务必参考这些信息来规划大纲，使其切合当前的市场现状或技术事实，而不是凭空捏造。
例如：如果调研显示"某技术已过时"，则不要将其作为核心推荐。

## 输出规范
请严格按照以下JSON格式输出，结果用[PPT_OUTLINE]和[/PPT_OUTLINE]包裹：

[PPT_OUTLINE]
{
  "ppt_outline": {
    "cover": {
      "title": "引人注目的主标题",
      "sub_title": "副标题",
      "content": []
    },
    "table_of_contents": {
      "title": "目录",
      "content": ["第一部分标题", "第二部分标题", "..."]
    },
    "parts": [
      {
        "part_title": "第一部分：章节标题",
        "pages": [
          { "title": "页面标题1", "content": [] },
          { "title": "页面标题2", "content": [] }
        ]
      }
    ],
    "end_page": {
      "title": "总结与展望",
      "content": []
    }
  }
}
[/PPT_OUTLINE]

## Constraints
1. 必须严格遵循JSON格式。
2. 页数要求：{{PAGE_REQUIREMENTS}}
```

---

## 2. Bento Grid 布局规范（Step 4 & 5 使用）

```
内容页的便当网格 (Bento Grid) 布局
这是一种灵活的网格系统，其布局应由内容本身的需求驱动，而非僵硬的模板。通过组合不同尺寸的卡片，创造出动态且视觉有趣的布局。

- 核心原则:
    - 灵活性: 卡片数量不固定。可以是 1, 2, 3, 4, 5 或更多个，取决于如何更好地呈现信息。
    - 层级感: 使用卡片尺寸建立视觉层级。最重要的信息放在最大的卡片上。
    - 留白: 在所有卡片之间保持至少 20px 的间距。
    - 无重叠: 卡片不得相互覆盖

- 布局组合示例:
    - 单一焦点: 一张大卡片覆盖大部分区域 (w=1200, h=580)。适用于单一、有力的信息或详细的图表。
    - 两栏布局:
        - 50/50 对称: 两张等宽的卡片。
        - 非对称: 一张较宽的卡片（如 2/3 宽度）用于主内容，一张较窄的（1/3 宽度）用于辅助信息、数据或图片。
    - 三栏布局: 三张等宽的卡片，适合并列比较三项内容。
    - 主次结合: 一张大的居中卡片，两侧各一张小的垂直卡片。
    - 顶部英雄式: 顶部一张宽幅"英雄"卡片，下方是 2-4 个较小的等宽卡片网格。
    - 混合网格 (自由度最高): 自由混合各种尺寸的卡片，例如一个中等方块、两个小的水平矩形和一个垂直矩形。
```

---

## 3. SVG 设计稿生成 Prompt（Step 5 使用）

```
作为精通信息架构与 SVG 编码的专家，你的任务是将完整的文字内容转化为一张高质量、结构化、具备高级感、简洁感和专业感的 SVG 演示文稿页面。要求如下：

1. 画布: SVG viewBox 必须是 0 0 1280 720。

2. 风格配色（严格遵循用户选择的风格）：
   - 科技感: 深色背景(#1a1a2e)、渐变蓝紫、霓虹色强调
   - 商务: 白/浅灰底(#f8f9fa)、深蓝(#1a3a5c)主色、金色(#d4a853)点缀
   - 学术: 纯白底(#ffffff)、深灰(#333333)文字、蓝色(#2563eb)强调
   - 创意: 大胆配色、非对称、视觉冲击
   - 极简: 大量留白、黑白灰、细线

3. 布局: 使用 Bento Grid 便当网格布局，卡片数量、尺寸、位置由内容驱动。

4. 设计质量:
   - 圆角矩形卡片（rx=12~16）
   - 卡片添加微阴影（filter="url(#shadow)"）
   - 标题使用 28-36px 粗体
   - 正文使用 16-20px 常规
   - 数据/数字使用 36-48px 粗体突出
   - 图标使用 SVG path 简单几何图形
   - 配色一致，不超过 4 种主色

5. 必须包含:
   - 页面标题（左上角或顶部，32px+）
   - 页码（右下角，14px）
   - 所有卡片内的文字内容完整可读
   - SVG 代码可直接复制使用

6. 禁止:
   - 元素重叠
   - 文字溢出卡片边界
   - 使用外部图片引用
   - 使用 JavaScript 或动画

请你根据我的内容输出SVG代码，我的内容是：
```

---

## 4. 风格配色方案速查

| 风格 | 背景色 | 主色 | 辅助色 | 强调色 |
|------|--------|------|--------|--------|
| 🔵 科技感 | #1a1a2e | #16213e | #0f3460 | #e94560 |
| 🟢 商务 | #f8f9fa | #1a3a5c | #2c5f8a | #d4a853 |
| 🟡 学术 | #ffffff | #333333 | #2563eb | #1e40af |
| 🟣 创意 | #faf5ff | #7c3aed | #db2777 | #f59e0b |
| ⚪ 极简 | #ffffff | #111111 | #666666 | #000000 |

---

## 5. 质量检查清单

每页设计稿生成后必须自检：

- [ ] 卡片之间间距 ≥ 20px
- [ ] 无元素重叠
- [ ] 文字完整可读，无截断
- [ ] 配色与 Step 1 选择的风格一致
- [ ] 内容准确，来自 Step 3 搜索素材
- [ ] 布局与 Step 4 策划稿一致
- [ ] SVG viewBox = 0 0 1280 720

---

## 6. 常见问题处理

### SVG 元素重叠
- 检查每个卡片的 x, y, width, height 是否超出画布
- 确保卡片间距 ≥ 20px
- 使用 group 标签组织卡片，避免坐标计算错误

### 文字溢出
- 使用 textLength 和 lengthAdjust 控制文字宽度
- 长文本使用多个 tspan 换行
- 卡片宽度至少是文字预估宽度的 1.2 倍

### 风格不一致
- 严格从配色方案速查表中选取颜色
- 不超过 4 种主色（背景色不算）
- 同一 PPT 内所有页面的配色方案保持一致